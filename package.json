{
  "devDependencies": {
    "turbo": "^1.10.15"
  },
  "scripts": {
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "generate-api": "openapi-generator-cli generate -i http://localhost:3000/api-json -g typescript-axios -o ./apps/fe/src/api"
  },
  "workspaces":[
    "apps/*"
  ]
}
